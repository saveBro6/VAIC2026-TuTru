import { Bell, ChevronDown, LogOut, Menu, Search } from 'lucide-react'
import { useAuth } from '../../hooks/useAuth'
export function AppHeader({ onMenu }: { onMenu: () => void }) { const { user, logout } = useAuth(); const initials = user?.full_name.split(' ').slice(-2).map((word) => word[0]).join('') ?? 'MF'; return <header className="sticky top-0 z-30 flex h-[72px] items-center justify-between border-b border-border/80 bg-card/85 px-4 backdrop-blur-xl sm:px-6 lg:px-8"><div className="flex items-center gap-3"><button className="rounded-xl border border-border bg-card p-2.5 shadow-sm lg:hidden" aria-label="Mở menu" onClick={onMenu}><Menu size={20}/></button><div className="relative hidden w-72 xl:block"><Search className="absolute left-3 top-2.5 text-muted-foreground" size={18}/><input className="h-10 w-full rounded-xl border border-border bg-muted/50 pl-10 pr-3 text-sm outline-none focus:border-primary focus:bg-card focus:ring-4 focus:ring-primary/10" placeholder="Tìm nhanh trong hệ thống..."/></div><div className="hidden lg:block xl:hidden"><p className="text-xs font-bold uppercase tracking-widest text-primary">Smart Hospital</p><p className="font-semibold text-foreground">Trung tâm điều phối</p></div></div><div className="flex items-center gap-2"><div className="mr-2 hidden items-center gap-2 rounded-full border border-emerald-200 bg-emerald-50 px-3 py-1.5 text-xs font-semibold text-emerald-700 sm:flex"><span className="h-2 w-2 animate-pulse rounded-full bg-emerald-500"/>Hệ thống ổn định</div><button aria-label="Thông báo" className="relative rounded-xl p-2.5 text-muted-foreground transition hover:bg-muted hover:text-foreground"><Bell size={20}/><span className="absolute right-2 top-2 h-2 w-2 rounded-full bg-destructive ring-2 ring-white"/></button><button className="flex items-center gap-3 rounded-xl p-1.5 text-left transition hover:bg-muted"><span className="grid h-9 w-9 place-items-center rounded-xl bg-gradient-to-br from-primary to-cyan-600 text-xs font-bold text-white shadow-md shadow-primary/20">{initials}</span><span className="hidden md:block"><span className="block text-sm font-semibold leading-4 text-foreground">{user?.full_name}</span><span className="text-xs text-muted-foreground">{user?.role}</span></span><ChevronDown size={16} className="hidden text-muted-foreground md:block"/></button><button aria-label="Đăng xuất" onClick={logout} className="rounded-xl p-2.5 text-muted-foreground transition hover:bg-red-50 hover:text-destructive"><LogOut size={19}/></button></div></header> }
