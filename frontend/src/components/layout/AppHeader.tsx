import { Bell, ChevronDown, LogOut, Menu, Search } from 'lucide-react'
import { useAuth } from '../../hooks/useAuth'
export function AppHeader({ onMenu }: { onMenu: () => void }) { const { user, logout } = useAuth(); const initials = user?.full_name.split(' ').slice(-2).map((word) => word[0]).join('') ?? 'AT'; return <header className="sticky top-0 z-30 flex h-[72px] items-center justify-between border-b border-border bg-white px-4 sm:px-6 lg:px-8"><div className="flex items-center gap-3"><button className="rounded-lg border border-border bg-card p-2.5 lg:hidden" aria-label="Mở menu" onClick={onMenu}><Menu size={20}/></button><div className="relative hidden w-72 xl:block"><Search className="pointer-events-none absolute left-4 top-2.5 text-muted-foreground" size={18}/><input className="h-10 w-full rounded-lg border border-border bg-slate-50 pl-12 pr-4 text-sm outline-none focus:border-primary focus:bg-card focus:ring-4 focus:ring-primary/10" placeholder="Tìm bệnh nhân, phòng khám..."/></div><div className="hidden lg:block xl:hidden"><p className="text-xs font-bold uppercase tracking-widest text-primary">Bệnh viện An Tâm</p><p className="font-semibold text-foreground">Điều phối khám bệnh</p></div></div><div className="flex items-center gap-2"><div className="mr-2 hidden items-center gap-2 rounded-md border border-sky-200 bg-sky-50 px-3 py-1.5 text-xs font-semibold text-sky-700 sm:flex"><span className="h-2 w-2 rounded-full bg-[#ea7a50]"/>Đang hoạt động</div><button aria-label="Thông báo" className="relative rounded-lg p-2.5 text-muted-foreground transition hover:bg-muted hover:text-foreground"><Bell size={20}/><span className="absolute right-2 top-2 h-2 w-2 rounded-full bg-destructive ring-2 ring-white"/></button><button className="flex items-center gap-3 rounded-lg p-1.5 text-left transition hover:bg-muted"><span className="grid h-9 w-9 place-items-center rounded-lg bg-[#176b9b] text-xs font-bold text-white">{initials}</span><span className="hidden md:block"><span className="block text-sm font-semibold leading-4 text-foreground">{user?.full_name}</span><span className="text-xs text-muted-foreground">{user?.role === 'DOCTOR' ? 'Nhân viên y tế' : user?.role === 'PATIENT' ? 'Bệnh nhân' : 'Vận hành'}</span></span><ChevronDown size={16} className="hidden text-muted-foreground md:block"/></button><button aria-label="Đăng xuất" onClick={logout} className="rounded-lg p-2.5 text-muted-foreground transition hover:bg-red-50 hover:text-destructive"><LogOut size={19}/></button></div></header> }
