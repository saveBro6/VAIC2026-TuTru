import { ArrowRight, Ticket, Users } from 'lucide-react'
import { Progress } from '../ui/progress'
export function CurrentQueueCard({ queueNumber = 'A042', ahead = 3 }: { queueNumber?: string; ahead?: number }) { return <div className="card group"><div className="flex items-center justify-between"><span className="icon-box"><Ticket/></span><span className="rounded-full bg-amber-50 px-2.5 py-1 text-xs font-semibold text-amber-700">Đang chờ</span></div><p className="mt-5 text-sm font-medium text-muted-foreground">Số thứ tự của bạn</p><p className="mt-1 text-4xl font-black tracking-tight text-foreground">{queueNumber}</p><div className="mt-5 flex items-center justify-between text-sm"><span className="flex items-center gap-2 text-muted-foreground"><Users size={16}/>{ahead} bệnh nhân phía trước</span><ArrowRight size={17} className="text-primary transition group-hover:translate-x-1"/></div><Progress value={72} className="mt-3"/></div> }
