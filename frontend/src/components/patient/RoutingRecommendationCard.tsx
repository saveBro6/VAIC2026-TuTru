import { motion } from 'framer-motion'
import { BrainCircuit, Building2, Clock3, MapPin, ShieldCheck, Sparkles, Users } from 'lucide-react'
import type { AIRecommendation } from '../../types'
import { Progress } from '../ui/progress'
export function RoutingRecommendationCard({ recommendation }: { recommendation: AIRecommendation }) { const confidence = Math.round(recommendation.confidence * 100); return <motion.section initial={{ opacity: 0, scale: .99 }} animate={{ opacity: 1, scale: 1 }} className="relative overflow-hidden rounded-3xl border border-primary/20 bg-card shadow-[0_20px_60px_rgba(8,127,120,.1)]"><div className="absolute inset-x-0 top-0 h-1 bg-gradient-to-r from-primary via-cyan-500 to-emerald-500"/><div className="grid lg:grid-cols-[1fr_280px]"><div className="p-6 sm:p-8"><span className="inline-flex items-center gap-2 rounded-full border border-primary/15 bg-primary/5 px-3 py-1.5 text-xs font-bold uppercase tracking-wider text-primary"><Sparkles size={14}/>Đề xuất điều phối ưu tiên</span><h2 className="mt-5 text-3xl font-black tracking-tight text-foreground">{recommendation.department}</h2><p className="mt-2 text-xl font-semibold text-primary">{recommendation.room}</p><div className="mt-6 grid gap-3 sm:grid-cols-3"><Info icon={<MapPin/>} label="Vị trí" value={`Tầng ${recommendation.floor}`}/><Info icon={<Users/>} label="Hàng đợi" value={`${recommendation.waitingCount} người`}/><Info icon={<Clock3/>} label="Chờ dự kiến" value={`${recommendation.estimatedWait} phút`}/></div><div className="mt-6 rounded-2xl border border-border bg-muted/40 p-4"><p className="flex items-center gap-2 text-sm font-bold text-foreground"><BrainCircuit size={17} className="text-primary"/>Cơ sở đề xuất</p><p className="mt-2 text-sm leading-6 text-muted-foreground">{recommendation.reason}</p></div></div><aside className="flex flex-col justify-between border-t border-border bg-[#f2faf9] p-6 lg:border-l lg:border-t-0"><div><span className="icon-box"><Building2/></span><p className="mt-5 text-sm font-semibold text-muted-foreground">Độ tin cậy mô hình</p><p className="mt-1 text-5xl font-black tracking-tight text-primary">{confidence}<span className="text-xl">%</span></p><Progress value={confidence} className="mt-4"/><p className="mt-3 text-xs leading-5 text-muted-foreground">Mức tin cậy cao dựa trên các tín hiệu điều phối hiện tại.</p></div><div className="mt-6 flex gap-2 rounded-xl border border-amber-200 bg-amber-50 p-3 text-xs leading-5 text-amber-800"><ShieldCheck className="shrink-0" size={18}/><span>Đề xuất hỗ trợ điều phối, không thay thế chẩn đoán hoặc quyết định y khoa.</span></div></aside></div></motion.section> }
function Info({ icon, label, value }: { icon: React.ReactNode; label: string; value: string }) { return <div className="rounded-2xl border border-border bg-card p-4"><span className="text-primary [&>svg]:h-5 [&>svg]:w-5">{icon}</span><p className="mt-3 text-xs font-medium text-muted-foreground">{label}</p><p className="mt-0.5 font-bold text-foreground">{value}</p></div> }
