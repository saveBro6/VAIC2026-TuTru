import { zodResolver } from '@hookform/resolvers/zod'
import { useForm } from 'react-hook-form'
import { checkinSchema, type CheckinFormData } from '../../schemas/checkinSchema'
import { AppButton } from '../common/AppButton'
export function CheckinForm({ onSubmit, loading }: { onSubmit: (data: CheckinFormData) => void; loading?: boolean }) { const { register, handleSubmit, formState: { errors } } = useForm<CheckinFormData>({ resolver: zodResolver(checkinSchema), defaultValues: { examinationType: 'GENERAL', patientType: 'INSURANCE' } }); return <form className="card space-y-5" onSubmit={handleSubmit(onSubmit)}><Field label="Loại khám" error={errors.examinationType?.message}><select {...register('examinationType')} className="form-control"><option value="GENERAL">Khám tổng quát</option><option value="SPECIALIST">Khám chuyên khoa</option><option value="FOLLOW_UP">Tái khám</option></select></Field><Field label="Loại bệnh nhân" error={errors.patientType?.message}><select {...register('patientType')} className="form-control"><option value="INSURANCE">Có bảo hiểm y tế</option><option value="SERVICE">Khám dịch vụ</option><option value="PRIORITY">Đối tượng ưu tiên</option></select></Field><AppButton type="submit" loading={loading} className="w-full text-lg">Xác nhận check-in</AppButton></form> }
function Field({ label, error, children }: { label: string; error?: string; children: React.ReactNode }) { return <label className="block"><span className="mb-1.5 block text-sm font-bold">{label}</span>{children}{error && <span className="text-sm text-red-600">{error}</span>}</label> }
