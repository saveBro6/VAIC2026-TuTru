import { RotateCcw, Search } from 'lucide-react'
import type { Priority, VisitStatus } from '../../types'

export interface VisitFilters { search: string; status: VisitStatus | 'ALL'; priority: Priority | 'ALL'; department: string }
export function VisitStatusFilter({ filters, onChange, onReset }: { filters: VisitFilters; onChange: (filters: VisitFilters) => void; onReset: () => void }) { return <div className="card mb-4 grid gap-3 md:grid-cols-2 xl:grid-cols-[1fr_190px_180px_190px_auto]"><label className="relative"><Search className="absolute left-3 top-3 text-slate-400" size={20}/><input value={filters.search} onChange={(e) => onChange({ ...filters, search: e.target.value })} placeholder="Tên, mã lượt, số thứ tự..." className="form-control pl-10"/></label><select aria-label="Lọc trạng thái" value={filters.status} onChange={(e) => onChange({ ...filters, status: e.target.value as VisitFilters['status'] })} className="form-control"><option value="ALL">Tất cả trạng thái</option><option value="WAITING">Đang chờ</option><option value="CALLED">Đã gọi</option><option value="IN_EXAMINATION">Đang khám</option><option value="COMPLETED">Hoàn thành</option></select><select aria-label="Lọc ưu tiên" value={filters.priority} onChange={(e) => onChange({ ...filters, priority: e.target.value as VisitFilters['priority'] })} className="form-control"><option value="ALL">Tất cả ưu tiên</option><option value="EMERGENCY">Cấp cứu</option><option value="URGENT">Khẩn cấp</option><option value="HIGH">Ưu tiên cao</option><option value="NORMAL">Bình thường</option></select><select aria-label="Lọc khoa" value={filters.department} onChange={(e) => onChange({ ...filters, department: e.target.value })} className="form-control"><option value="ALL">Tất cả khoa</option><option value="Nội tổng hợp">Nội tổng hợp</option><option value="Xét nghiệm">Xét nghiệm</option></select><button onClick={onReset} className="flex min-h-11 items-center justify-center gap-2 rounded-xl border border-slate-200 px-3 font-semibold hover:bg-slate-50"><RotateCcw size={18}/>Đặt lại</button></div> }
