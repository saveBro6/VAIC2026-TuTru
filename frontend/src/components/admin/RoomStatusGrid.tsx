import { MoreHorizontal, Users } from 'lucide-react'
import type { Room } from '../../types'
import { Badge } from '../common/Badge'
export function RoomStatusGrid({ rooms, onStatus }: { rooms: Room[]; onStatus?: (id: string, status: Room['status']) => void }) { const labels = { OPEN: 'Đang mở', PAUSED: 'Tạm dừng', CLOSED: 'Đã đóng' }; return <div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{rooms.map((room) => <div className="card" key={room.id}><div className="flex justify-between"><div><p className="text-xs font-bold uppercase text-teal-700">Phòng {room.code}</p><h3 className="text-lg font-extrabold">{room.name}</h3><p className="text-sm text-slate-500">{room.department} · Tầng {room.floor}</p></div><button aria-label="Thao tác phòng" className="h-10 rounded-lg p-2 hover:bg-slate-100"><MoreHorizontal/></button></div><div className="mt-5 grid grid-cols-2 gap-3"><div className="rounded-xl bg-slate-50 p-3"><Users size={18}/><p className="mt-2 text-2xl font-black">{room.waitingCount}</p><p className="text-xs text-slate-500">đang chờ</p></div><div className="rounded-xl bg-slate-50 p-3"><p className="text-2xl font-black">{room.averageWait}'</p><p className="text-xs text-slate-500">chờ trung bình</p></div></div><div className="mt-4 flex items-center justify-between"><Badge className={room.status === 'OPEN' ? 'bg-emerald-100 text-emerald-800' : room.status === 'PAUSED' ? 'bg-amber-100 text-amber-800' : 'bg-slate-100'}>{labels[room.status]}</Badge><select aria-label="Cập nhật trạng thái" value={room.status} onChange={(e) => onStatus?.(room.id, e.target.value as Room['status'])} className="rounded-lg border border-slate-200 p-2 text-sm"><option value="OPEN">Mở phòng</option><option value="PAUSED">Tạm dừng</option><option value="CLOSED">Đóng phòng</option></select></div></div>)}</div> }
